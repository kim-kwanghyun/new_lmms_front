# 리눅스 서버에서 사용하지 않는 패키지 설치 방지
# optional dependencies는 실패해도 계속 진행
optional=true

# oxc-parser 관련 패키지는 설치하지 않음 (선택적 의존성으로 처리)
# 이 설정은 npm install 시 oxc-parser 관련 네이티브 바인딩 설치를 건너뜀

# 프로덕션 빌드 시 devDependencies 설치 안 함
production=false

